"""Database layer: ORM models (models.py) and async engine/session
helpers (session.py). Both backend and agent_loop import from here so
there is exactly one definition of what a `Session` row (etc.) looks
like."""
